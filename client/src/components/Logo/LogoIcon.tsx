import React from 'react';

import './Logo.scss';

const LogoIcon = () => {
  return (
    <img
      className="logo--icon"
      src="/logo-icon.png"
      alt="We Should Watch Logo"
    />
  );
};

export default LogoIcon;
